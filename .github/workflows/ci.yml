name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build-test:
    runs-on: ubuntu-latest
    env:
      # bun install resolves @cuny-ai-lab/* from GitHub Packages via the
      # committed .npmrc, which reads this token.
      NODE_AUTH_TOKEN: ${{ secrets.CAIL_PACKAGES_TOKEN }}
    steps:
      - uses: actions/checkout@v4

      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.5

      - name: Install
        run: bun install --frozen-lockfile

      - name: Typecheck
        run: bun run typecheck

      - name: Build
        run: bun run build

      - name: Verify committed build matches source
        run: |
          git diff --exit-code -- dist
          test -z "$(git status --porcelain --untracked-files=all -- dist)"

      - name: Verify package contents
        run: bun pm pack --dry-run

      - name: Test
        run: bun run test
